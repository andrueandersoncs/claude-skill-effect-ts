# Loops & Mutation

NEVER use for/while/do-while loops or mutate variables. Use Effect's Array/Record modules for data transformation, Effect.forEach for effectful iteration, and recursion for complex traversals. All data transformations must be immutable.
